#include<stdio.h>
int main()
{
	printf("Hello World");
	return 0;
}

// use first, gcc helloc.c
// then,      After sometime an application of type exe, with some name appears e.g, "a", then enter a


//Method 2 //Creating application name according to use
// use, gcc -o hello helloc.c
// this will create an application of name hello.exe at store location
// then use, hello to get output